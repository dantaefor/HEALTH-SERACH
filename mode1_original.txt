/* ----------------- MODE 1: SEARCH SPEC MATCH RES ----------------- */
        <div className="grid grid-cols-1 lg:grid-cols-12 gap-8 items-start">
          
          {/* Left Column (Inputs & Results) */}
          <div className="lg:col-span-7 space-y-6">
            
            {/* TOP SECTION: 12 Specifications Fields */}
            <div className="bg-white rounded-3xl shadow-[0_2px_10px_-3px_rgba(6,81,237,0.1)] p-5 space-y-5">
            <div className="flex items-center justify-between border-b border-indigo-50 pb-3">
              <div className="flex items-center gap-2">
                <Sparkles className="w-4 h-4 text-[#aa2d00]" />
                <h3 className="text-sm font-medium text-[#181d26]">12가지 핵심 사양 기입</h3>
              </div>
              <button
                onClick={() => setShowSliders(!showSliders)}
                className={`text-[11px] font-semibold border rounded-lg px-2.5 py-1 flex items-center gap-1 cursor-pointer transition-all ${showSliders ? "bg-[#f5e9d4] border-[#ebdfca] text-[#181d26]" : "bg-white border-[#dddddd] text-slate-600 hover:bg-slate-50"}`}
              >
                <Sliders className="w-3 h-3 text-[#181d26]" />
                가중치 조절 {showSliders ? "닫기" : "열기"}
              </button>
            </div>

            {showSliders && (
              <div className="bg-indigo-50/50 rounded-2xl p-4 border border-indigo-50 space-y-3.5 animate-fadeIn">
                <div className="flex items-center justify-between">
                  <span className="text-[10px] font-mono font-bold text-indigo-700 uppercase">가중치 비율 수동 튜닝</span>
                  <button
                    onClick={handleResetWeights}
                    className="text-[9px] font-bold text-indigo-600 underline hover:text-indigo-800 cursor-pointer"
                  >
                    초기화
                  </button>
                </div>
                
                <div className="grid grid-cols-1 sm:grid-cols-3 lg:grid-cols-5 gap-x-4 gap-y-3 text-xs pr-1">
                  {Object.entries(gWeights).map(([field, weight]) => {
                    const labelMap: Record<string, string> = {
                      gfa: "연면적", beds: "병상수", constructionCost: "공사비", designFee: "설계비",
                      category: "용도분류", isPublic: "민간/공공", location: "예정지역", procurementMethod: "발주방식",
                      status: "진행단계", openingYear: "준공년도", designYear: "설계연도", designer: "설계사",
                      contractor: "시공사", cladding: "입면마감", scale: "규모층수"
                    };
                    return (
                      <div key={field} className="space-y-1 bg-white border border-slate-50 shadow-sm p-2 rounded-xl">
                        <div className="flex justify-between items-center text-[10px]">
                          <span className="font-semibold text-[#181d26]">{labelMap[field] || field}</span>
                          <span className="font-mono font-bold text-[#aa2d00]">{(weight * 100).toFixed(1)}%</span>
                        </div>
                        <input
                          type="range"
                          min="0.005"
                          max="0.4"
                          step="0.005"
                          value={weight}
                          onChange={(e) => handleSliderChange(field, parseFloat(e.target.value))}
                          className="w-full h-1 bg-slate-200 rounded-lg appearance-none cursor-pointer accent-[#aa2d00]"
                        />
                      </div>
                    );
                  })}
                </div>
              </div>
            )}

            {/* Input Grid Container - Horizontal Layout (4 columns on large screens) */}
            <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
              
              {/* 1. 연면적 */}
              <div>
                <label className="block text-xs font-bold text-slate-800 mb-1.5">1) 계획 연면적 (m²)</label>
                <div className="relative">
                  <input
                    type="number"
                    value={m1Gfa || ""}
                    onChange={(e) => setM1Gfa(Math.max(0, parseFloat(e.target.value) || 0))}
                    className="w-full bg-slate-50 border-none rounded-xl focus:ring-2 focus:ring-indigo-100 transition-shadow font-medium pl-3 pr-10 py-1.5 text-xs text-slate-950 font-bold focus:outline-none focus:ring-1.5 focus:ring-indigo-500 transition-all"
                  />
                  <span className="absolute right-2.5 top-1/2 -translate-y-1/2 text-xs font-bold text-slate-700 font-mono">m²</span>
                </div>
                {m1Gfa > 0 && (
                  <p className="text-xs text-[#064312] font-semibold mt-1 font-mono">약 {formatPyung(m1Gfa)} 평형 스케일</p>
                )}
              </div>

              {/* 2. 병상수 */}
              <div>
                <label className="block text-xs font-bold text-slate-800 mb-1.5">2) 계획 병상수 (beds)</label>
                <div className="relative">
                  <input
                    type="number"
                    value={m1Beds || ""}
                    onChange={(e) => setM1Beds(Math.max(0, parseInt(e.target.value) || 0))}
                    className="w-full bg-slate-50 border-none rounded-xl focus:ring-2 focus:ring-indigo-100 transition-shadow font-medium pl-3 pr-10 py-1.5 text-xs text-slate-950 font-bold focus:outline-none focus:ring-1.5 focus:ring-indigo-500 transition-all"
                  />
                  <span className="absolute right-2.5 top-1/2 -translate-y-1/2 text-xs font-bold text-slate-700">병상</span>
                </div>
              </div>

              {/* 3. 공사비 */}
              <div>
                <label className="block text-xs font-bold text-slate-800 mb-1.5">3) 계획 공사비 (백만원)</label>
                <div className="relative">
                  <input
                    type="number"
                    value={m1Cost || ""}
                    onChange={(e) => setM1Cost(Math.max(0, parseInt(e.target.value) || 0))}
                    className="w-full bg-slate-50 border-none rounded-xl focus:ring-2 focus:ring-indigo-100 transition-shadow font-medium pl-3 pr-16 py-1.5 text-xs text-slate-950 font-bold focus:outline-none focus:ring-1.5 focus:ring-indigo-500 transition-all"
                  />
                  <span className="absolute right-2.5 top-1/2 -translate-y-1/2 text-xs font-bold text-slate-750">백만원</span>
                </div>
              </div>

              {/* 4. 설계비 */}
              <div>
                <label className="block text-xs font-bold text-slate-800 mb-1.5">4) 계획 설계비 (백만원)</label>
                <div className="relative">
                  <input
                    type="number"
                    value={m1DesignFee || ""}
                    onChange={(e) => setM1DesignFee(Math.max(0, parseInt(e.target.value) || 0))}
                    className="w-full bg-slate-50 border-none rounded-xl focus:ring-2 focus:ring-indigo-100 transition-shadow font-medium pl-3 pr-12 py-1.5 text-xs text-slate-950 font-bold focus:outline-none focus:ring-1.5 focus:ring-indigo-500 transition-all"
                  />
                  <span className="absolute right-2.5 top-1/2 -translate-y-1/2 text-xs font-bold text-slate-750">백만원</span>
                </div>
              </div>

              {/* 5. 규모 (지하/지상) */}
              <div>
                <label className="block text-xs font-bold text-slate-800 mb-1.5">5) 건물 층수 규모 범위</label>
                <div className="grid grid-cols-2 gap-2">
                  <div className="relative">
                    <input
                      type="number"
                      placeholder="지하"
                      value={m1ScaleB || ""}
                      onChange={(e) => setM1ScaleB(Math.max(0, parseInt(e.target.value) || 0))}
                      className="w-full bg-slate-50 border-none rounded-xl focus:ring-2 focus:ring-indigo-100 transition-shadow font-medium pl-3 pr-6 py-1.5 text-xs text-slate-950 font-bold"
                    />
                    <span className="absolute right-2.5 top-1/2 -translate-y-1/2 text-xs font-bold text-slate-600">B</span>
                  </div>
                  <div className="relative">
                    <input
                      type="number"
                      placeholder="지상"
                      value={m1ScaleF || ""}
                      onChange={(e) => setM1ScaleF(Math.max(0, parseInt(e.target.value) || 0))}
                      className="w-full bg-slate-50 border-none rounded-xl focus:ring-2 focus:ring-indigo-100 transition-shadow font-medium pl-3 pr-6 py-1.5 text-xs text-slate-950 font-bold"
                    />
                    <span className="absolute right-2.5 top-1/2 -translate-y-1/2 text-xs font-bold text-slate-600">F</span>
                  </div>
                </div>
              </div>

              {/* 6. 입면주요마감 */}
              <div>
                <label className="block text-xs font-bold text-slate-800 mb-1.5">6) 입면 주요 마감재</label>
                <select
                  value={m1Cladding}
                  onChange={(e) => setM1Cladding(e.target.value)}
                  className="w-full bg-slate-50 border-none rounded-xl focus:ring-2 focus:ring-indigo-100 transition-shadow font-medium px-2.5 py-1.5 text-xs text-slate-950 font-bold focus:outline-none"
                >
                  <option value="N/A">선택안함 (전체)</option>
                  {allCladdings.map(c => <option key={c} value={c}>{c}</option>)}
                </select>
              </div>

              {/* 7. 위치 */}
              <div>
                <label className="block text-xs font-bold text-slate-800 mb-1.5">7) 예정 건립 지역</label>
                <select
                  value={m1Location}
                  onChange={(e) => setM1Location(e.target.value)}
                  className="w-full bg-slate-50 border-none rounded-xl focus:ring-2 focus:ring-indigo-100 transition-shadow font-medium px-2.5 py-1.5 text-xs text-slate-950 font-bold focus:outline-none"
                >
                  <option value="">선택안함 (전체)</option>
                  {allLocations.map(l => <option key={l} value={l}>{l}</option>)}
                </select>
              </div>

              {/* 8. 공공/민간 */}
              <div>
                <label className="block text-xs font-bold text-slate-800 mb-1.5">8) 공공 / 민간 구분</label>
                <div className="grid grid-cols-2 gap-2">
                  <button
                    onClick={() => setM1IsPublic(true)}
                    className={`py-1.5 text-xs rounded-lg border-2 font-bold cursor-pointer transition-all ${m1IsPublic ? "bg-[#e0f1fe] border-[#0284c7] text-[#0369a1]" : "bg-slate-50 border-[#a5b0ba] text-slate-800 hover:bg-slate-100"}`}
                  >
                    공공 (보조)
                  </button>
                  <button
                    onClick={() => setM1IsPublic(false)}
                    className={`py-1.5 text-xs rounded-lg border-2 font-bold cursor-pointer transition-all ${!m1IsPublic ? "bg-[#ccfbf1] border-[#0d9488] text-[#0f766e]" : "bg-slate-50 border-[#a5b0ba] text-slate-800 hover:bg-slate-100"}`}
                  >
                    민간 (자체)
                  </button>
                </div>
              </div>

              {/* 9. 발주방식 */}
              <div>
                <label className="block text-xs font-bold text-slate-800 mb-1.5">9) 추진 발주 방식</label>
                <select
                  value={m1Procurement}
                  onChange={(e) => setM1Procurement(e.target.value)}
                  className="w-full bg-slate-50 border-none rounded-xl focus:ring-2 focus:ring-indigo-100 transition-shadow font-medium px-2.5 py-1.5 text-xs text-slate-950 font-bold focus:outline-none"
                >
                  <option value="N/A">선택안함 (전체)</option>
                  {allProcurements.map(p => <option key={p} value={p}>{p}</option>)}
                </select>
              </div>

              {/* 10. 분류 */}
              <div>
                <label className="block text-xs font-bold text-slate-800 mb-1.5">10) 의료용도 분류</label>
                <select
                  value={m1Category}
                  onChange={(e) => setM1Category(e.target.value)}
                  className="w-full bg-slate-50 border-none rounded-xl focus:ring-2 focus:ring-indigo-100 transition-shadow font-medium px-2.5 py-1.5 text-xs text-slate-950 font-bold focus:outline-none"
                >
                  <option value="">선택안함 (전체)</option>
                  {allCategories.map(c => <option key={c} value={c}>{c}</option>)}
                </select>
              </div>

              {/* 11. 진행단계 */}
              <div>
                <label className="block text-xs font-bold text-slate-800 mb-1.5">11) 기획 진행단계</label>
                <select
                  value={m1Status}
                  onChange={(e) => setM1Status(e.target.value)}
                  className="w-full bg-slate-50 border-none rounded-xl focus:ring-2 focus:ring-indigo-100 transition-shadow font-medium px-2.5 py-1.5 text-xs text-slate-950 font-bold focus:outline-none"
                >
                  <option value="N/A">선택안함 (전체)</option>
                  {allStatuses.map(s => <option key={s} value={s}>{s}</option>)}
                </select>
              </div>

              {/* 12. 시공사 */}
              <div>
                <label className="block text-xs font-bold text-slate-800 mb-1.5">12) 수주 예정 시공사</label>
                <select
                  value={m1Contractor}
                  onChange={(e) => setM1Contractor(e.target.value)}
                  className="w-full bg-slate-50 border-none rounded-xl focus:ring-2 focus:ring-indigo-100 transition-shadow font-medium px-2.5 py-1.5 text-xs text-slate-950 font-bold focus:outline-none"
                >
                  <option value="N/A">선택안함 (전체)</option>
                  {allContractors.map(c => <option key={c} value={c}>{c}</option>)}
                </select>
              </div>

            </div>
          </div>

          {/* BOTTOM SECTION: Results Suggestion Board TOP 1~3 directly in Left Column */}
          <div className="bg-slate-100 border border-slate-300 rounded-2xl p-5 shadow-3xs">
            <div className="flex items-center gap-1.5 mb-2">
              <span className="w-2.5 h-2.5 bg-emerald-500 rounded-full animate-pulse" />
              <h4 className="text-sm font-bold text-slate-900">지능형 최적 추천 모델 제안 (TOP 1 ~ 3)</h4>
            </div>
            <p className="text-xs text-slate-700 font-medium font-sans">기입된 12개 실시간 변인값에 부가 결합 가중치를 매칭하여 최우수 유사 건립 사례를 추천합니다.</p>
          </div>

          {loading ? (
            <div className="h-64 flex flex-col items-center justify-center text-slate-500 gap-3 bg-white border border-slate-250 rounded-2xl shadow-3xs">
              <RefreshCw className="w-8 h-8 text-indigo-600 animate-spin" />
              <span className="text-xs font-bold text-slate-700 animate-pulse">고속 비연계 가중치 재정규화 유사도 도출 중...</span>
            </div>
          ) : searchResults.length > 0 ? (
            <div className="grid grid-cols-1 gap-6">
              
              {/* TOP 1 - GOLD EMBLEM (Enlarged) */}
              {top1Value && (
                  <div
                    onClick={() => {
                      setDetailModalCase(top1Value);
                      onSelectCase(top1Value);
                    }}
                    className={`relative border-2 rounded-2xl p-6 shadow-md hover:shadow-lg transition-all cursor-pointer group ${
                      (top1Value.status === "취소" || top1Value.status === "용역중지")
                        ? "bg-pink-50/70 border-pink-450 text-pink-950"
                        : "bg-gradient-to-br from-amber-50/50 via-white to-amber-50/10 border-amber-400"
                    }`}
                  >
                    {/* Gold Badge & Cancelled Status */}
                    <div className="absolute top-4 right-4 flex flex-col items-end gap-1.5 select-none z-10">
                      <div className="bg-amber-600 text-white font-black text-xs px-3 py-1.5 rounded-lg flex items-center gap-1 shadow-sm">
                        <Award className="w-4 h-4" />
                        추천 1순위 (GOLD)
                      </div>
                      {(top1Value.status === "취소" || top1Value.status === "용역중지") && (
                        <span className="text-xs font-black text-[#ff00ff] bg-pink-50 border border-pink-300 px-2.5 py-1 rounded-md uppercase tracking-wide shadow-xs animate-pulse">
                          {top1Value.status}
                        </span>
                      )}
                    </div>

                    <div className="flex flex-col md:flex-row md:items-start gap-4">
                      {/* Rating Score */}
                      <div className="flex flex-col items-center justify-center border-2 border-amber-400 bg-amber-50 rounded-2xl p-3.5 w-24 text-center select-none shrink-0 shadow-3xs">
                        <span className="text-[11px] font-bold text-amber-900 uppercase tracking-wide">매칭점수</span>
                        <span className="text-3xl font-bold text-amber-700 font-mono mt-0.5">
                          {(top1Value.similarityScore * 100).toFixed(0)}
                        </span>
                        <span className="text-[11px] font-bold text-slate-800 mt-1.5 bg-white px-2 py-0.5 rounded-full border border-amber-350">
                          {top1Value.qualitativeDescription}
                        </span>
                      </div>

                      <div className="space-y-4 flex-1">
                        <div>
                          <span className="text-xs bg-indigo-100 text-indigo-900 border border-indigo-300 font-bold px-2.5 py-1 rounded-md uppercase shadow-3xs">
                            {top1Value.category}
                          </span>
                          <h4 className="text-xl font-bold text-slate-950 mt-3 flex items-center gap-1.5 group-hover:text-indigo-700 transition-colors">
                            {top1Value.projectName}
                            <span className="text-sm font-mono font-medium text-slate-500">({top1Value.designYear}년 설계)</span>
                          </h4>
                          <p className="text-[13px] text-slate-750 mt-1.5 font-medium">설계사: <strong className="text-slate-900">{top1Value.designer}</strong> | 발주처: <strong className="text-slate-900">{top1Value.client || "기밀/협회"}</strong> | 진행: <strong className="text-slate-900">{top1Value.status}</strong></p>
                        </div>

                        {/* Top 3 Contributing indicators */}
                        <div>
                          <p className="text-xs font-bold text-slate-700 uppercase tracking-wider">주요 일치 기여 요인 (Top 3)</p>
                          <div className="flex flex-wrap gap-2 mt-2">
                            {top1Value.topContributions?.map((cont: string, i: number) => (
                              <span key={i} className="text-xs font-bold font-sans bg-amber-100/80 text-amber-950 border border-amber-300 px-2.5 py-1 rounded-md shadow-3xs">
                                {cont}
                              </span>
                            ))}
                          </div>
                        </div>

                        {/* Reason narrative & Difference indicators */}
                        <div className="bg-slate-50 border border-slate-350 rounded-xl p-4 space-y-2 text-[13px] font-medium shadow-3xs">
                          {top1Value.matchingReasons?.map((reason: string, rIdx: number) => (
                            <p key={rIdx} className="text-slate-800 flex items-start gap-1.5 leading-relaxed">
                              <span className="text-emerald-600 font-bold mt-0.5">✔</span>
                              <span>{reason}</span>
                            </p>
                          ))}
                          {top1Value.keyDifferences?.map((diff: string, dIdx: number) => (
                            <p key={dIdx} className="text-slate-600 flex items-start gap-1.5 leading-relaxed">
                              <span className="text-slate-500 mt-0.5">📌</span>
                              <span className="font-sans text-xs">{diff}</span>
                            </p>
                          ))}
                        </div>

                        {/* Financial and metric comparison segment */}
                        <div className="grid grid-cols-2 sm:grid-cols-4 gap-3 pt-2">
                          <div className="bg-slate-100 rounded-lg p-2.5 border border-slate-300">
                            <p className="text-[11px] font-bold text-slate-600">연면적 (GFA)</p>
                            <p className="font-mono text-[13px] font-bold text-slate-900 mt-0.5">{top1Value.gfa?.toLocaleString()} m²</p>
                          </div>
                          <div className="bg-slate-100 rounded-lg p-2.5 border border-slate-300">
                            <p className="text-[11px] font-bold text-slate-600">병상 (Beds)</p>
                            <p className="font-mono text-[13px] font-bold text-slate-900 mt-0.5">{top1Value.beds} 병상</p>
                          </div>
                          <div className="bg-indigo-50/60 rounded-lg p-2.5 border border-indigo-250">
                            <p className="text-[11px] text-indigo-900 font-bold">총 공사비</p>
                            <p className="font-mono text-[13px] font-black text-indigo-700 mt-0.5">{top1Value.constructionCost ? `${top1Value.constructionCost.toLocaleString()} 백만원` : "공개 보류"}</p>
                          </div>
                          <div className="bg-slate-100 rounded-lg p-2.5 border border-slate-300">
                            <p className="text-[11px] font-bold text-slate-600">평당 단가</p>
                            <p className="font-mono text-[13px] font-bold text-emerald-700 mt-0.5">{top1Value.perPyungCost ? `${Math.round(top1Value.perPyungCost).toLocaleString()} 백만원/평` : "N/A"}</p>
                          </div>
                        </div>

                      </div>
                    </div>
                  </div>
                )}

                {/* SIBLING SUITE (TOP 2 & TOP 3) SIDE-BY-SIDE */}
                <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
                  
                  {/* TOP 2 - SILVER BADGE */}
                  {top2Value && (
                    <div
                      onClick={() => {
                        setDetailModalCase(top2Value);
                        onSelectCase(top2Value);
                      }}
                      className={`relative rounded-2xl p-5 shadow-sm hover:shadow-md transition-all cursor-pointer group ${
                        (top2Value.status === "취소" || top2Value.status === "용역중지")
                          ? "bg-pink-50/70 border-pink-350 text-pink-950"
                          : "bg-[#f8fafc] border-2 border-slate-500"
                      }`}
                    >
                      {/* Silver Badge & Cancelled Status */}
                      <div className="absolute top-3.5 right-3.5 flex flex-col items-end gap-1.5 select-none z-10">
                        <div className="bg-slate-600 text-white font-extrabold text-[11px] uppercase tracking-wider px-2.5 py-1.5 rounded-lg flex items-center gap-1 shadow-md">
                          <Award className="w-3.5 h-3.5" />
                          2순위 (SILVER)
                        </div>
                        {(top2Value.status === "취소" || top2Value.status === "용역중지") && (
                          <span className="text-xs font-black text-[#ff00ff] bg-pink-50 border border-pink-300 px-2.5 py-1 rounded-md uppercase tracking-wide shadow-xs animate-pulse">
                            {top2Value.status}
                          </span>
                        )}
                      </div>

                      <div className="space-y-4">
                        <div className="flex gap-3">
                          <div className="border-2 border-slate-500 bg-slate-200 rounded-xl p-2.5 w-16 text-center shrink-0 shadow-sm select-none">
                            <span className="text-[11px] font-black text-slate-700 uppercase block">Score</span>
                            <span className="text-base font-black font-mono text-slate-900">{(top2Value.similarityScore * 100).toFixed(0)}점</span>
                          </div>
                          <div>
                            <span className="text-xs font-bold bg-slate-300 text-slate-950 border-2 border-slate-500 px-2.5 py-1 rounded shadow-3xs uppercase">
                              {top2Value.category}
                            </span>
                            <h4 className="text-lg font-bold text-slate-950 mt-3 group-hover:text-indigo-800 transition-colors line-clamp-1">
                              {top2Value.projectName}
                            </h4>
                            <p className="text-xs text-slate-700 font-bold font-sans mt-1">{top2Value.designer} | {top2Value.location}</p>
                          </div>
                        </div>

                        {/* Top contributions */}
                        <div>
                          <p className="text-[11px] font-bold text-slate-700 uppercase tracking-wider">주요 일치 기여 요인</p>
                          <div className="flex flex-wrap gap-1.5 mt-1.5">
                            {top2Value.topContributions?.map((cont: string, i: number) => (
                              <span key={i} className="text-xs font-extrabold font-sans bg-slate-200 text-slate-950 px-2.5 py-1 rounded-md border-2 border-slate-400 shadow-3xs">
                                {cont}
                              </span>
                            ))}
                          </div>
                        </div>

                        {/* Reasons */}
                        <div className="bg-[#f1f5f9] border-2 border-slate-350 p-3.5 rounded-xl space-y-1.5 text-xs font-bold">
                          {top2Value.matchingReasons?.slice(0, 2).map((reason: string, rIdx: number) => (
                            <p key={rIdx} className="text-slate-900 flex items-start gap-1 leading-relaxed">
                              <span className="text-emerald-700 font-bold">✔</span>
                              <span>{reason}</span>
                            </p>
                          ))}
                        </div>

                        {/* Cost statistics */}
                        <div className="bg-[#f1f5f9] rounded-lg p-2.5 grid grid-cols-2 gap-2 text-center text-xs border-2 border-slate-350">
                          <div>
                            <p className="text-[11px] text-slate-750 font-black">병상 구조</p>
                            <p className="font-extrabold text-slate-950 mt-0.5">{top2Value.beds} 병상</p>
                          </div>
                          <div>
                            <p className="text-[11px] font-black text-indigo-900">예상 공사비</p>
                            <p className="font-black text-indigo-700 mt-0.5">{top2Value.constructionCost ? `${top2Value.constructionCost.toLocaleString()} 백만원` : "미정"}</p>
                          </div>
                        </div>

                      </div>
                    </div>
                  )}

                  {/* TOP 3 - BRONZE BADGE */}
                  {top3Value && (
                    <div
                      onClick={() => {
                        setDetailModalCase(top3Value);
                        onSelectCase(top3Value);
                      }}
                      className={`relative rounded-2xl p-5 shadow-sm hover:shadow-md transition-all cursor-pointer group ${
                        (top3Value.status === "취소" || top3Value.status === "용역중지")
                          ? "bg-pink-50/70 border-pink-350 text-pink-950"
                          : "bg-gradient-to-br from-amber-50/30 via-white to-amber-50/10 border-2 border-amber-600"
                      }`}
                    >
                      {/* Bronze Badge & Cancelled Status */}
                      <div className="absolute top-3.5 right-3.5 flex flex-col items-end gap-1.5 select-none z-10">
                        <div className="bg-[#b25e14] text-white font-extrabold text-[11px] uppercase tracking-wider px-2.5 py-1.5 rounded-lg flex items-center gap-1 shadow-md">
                          <Award className="w-3.5 h-3.5" />
                          3순위 (BRONZE)
                        </div>
                        {(top3Value.status === "취소" || top3Value.status === "용역중지") && (
                          <span className="text-xs font-black text-[#ff00ff] bg-pink-50 border border-pink-300 px-2.5 py-1 rounded-md uppercase tracking-wide shadow-xs animate-pulse">
                            {top3Value.status}
                          </span>
                        )}
                      </div>

                      <div className="space-y-4">
                        <div className="flex gap-3">
                          <div className="border-2 border-amber-550 bg-amber-100 rounded-xl p-2.5 w-16 text-center shrink-0 shadow-sm select-none">
                            <span className="text-[11px] font-black text-amber-800 uppercase block">Score</span>
                            <span className="text-base font-black font-mono text-amber-950">{(top3Value.similarityScore * 100).toFixed(0)}점</span>
                          </div>
                          <div>
                            <span className="text-xs font-bold bg-amber-100 text-amber-950 border-2 border-amber-500 px-2.5 py-1 rounded shadow-3xs uppercase">
                              {top3Value.category}
                            </span>
                            <h4 className="text-lg font-bold text-slate-950 mt-3 group-hover:text-indigo-800 transition-colors line-clamp-1">
                              {top3Value.projectName}
                            </h4>
                            <p className="text-xs text-slate-700 font-bold font-sans mt-1">{top3Value.designer} | {top3Value.location}</p>
                          </div>
                        </div>

                        {/* Top contributions */}
                        <div>
                          <p className="text-[11px] font-bold text-slate-700 uppercase tracking-wider">주요 일치 기여 요인</p>
                          <div className="flex flex-wrap gap-1.5 mt-1.5">
                            {top3Value.topContributions?.map((cont: string, i: number) => (
                              <span key={i} className="text-xs font-extrabold font-sans bg-amber-100 text-amber-950 border-2 border-amber-400 px-2.5 py-1 rounded-md shadow-3xs">
                                {cont}
                              </span>
                            ))}
                          </div>
                        </div>

                        {/* Reasons */}
                        <div className="bg-amber-50/60 p-3.5 rounded-xl space-y-1.5 text-xs font-bold border-2 border-amber-300">
                          {top3Value.matchingReasons?.slice(0, 2).map((reason: string, rIdx: number) => (
                            <p key={rIdx} className="text-slate-900 flex items-start gap-1 leading-relaxed">
                              <span className="text-emerald-700 font-bold">✔</span>
                              <span>{reason}</span>
                            </p>
                          ))}
                        </div>

                        {/* Cost statistics */}
                        <div className="bg-[#f1f5f9] rounded-lg p-2.5 grid grid-cols-2 gap-2 text-center text-xs border-2 border-slate-350">
                          <div>
                            <p className="text-[11px] text-slate-750 font-black">병상 구조</p>
                            <p className="font-extrabold text-slate-950 mt-0.5">{top3Value.beds} 병상</p>
                          </div>
                          <div>
                            <p className="text-[11px] font-black text-indigo-900">예상 공사비</p>
                            <p className="font-black text-indigo-700 mt-0.5">{top3Value.constructionCost ? `${top3Value.constructionCost.toLocaleString()} 백만원` : "미정"}</p>
                          </div>
                        </div>

                      </div>
                    </div>
                  )}

                </div>

                {/* Optional General results checklist overview underneath */}
                <div className="border border-slate-200 bg-white p-4 rounded-xl flex items-center justify-between text-xs">
                  <div className="flex items-center gap-2">
                    <HelpCircle className="w-4 h-4 text-slate-400" />
                    <span className="text-slate-500">기타 전체 유사 사례들을 10위까지 도출하고 싶으신가요?</span>
                  </div>
                  <button
                    onClick={() => {
                      onSearchModeChange("general");
                    }}
                    className="text-xs font-bold text-indigo-600 hover:text-indigo-800 flex items-center gap-1 cursor-pointer"
                  >
                    일반 다조건 검색에서 나열하기 <ChevronRight className="w-3.5 h-3.5" />
                  </button>
                </div>

              </div>
            ) : (
              <div className="bg-white border text-center p-12 text-slate-400 rounded-2xl">
                정보를 기입하면 유사도 정렬 계산 결과가 실시간 갱신됩니다.
              </div>
            )}
          </div> {/* Close Left Column (lg:col-span-7) */}

          {/* Right Column (lg:col-span-5): MapComponent */}
          <div className="lg:col-span-5 space-y-6 lg:sticky lg:top-6">
            {/* Korea Map Component */}
            {mapComponent}
          </div>
        </div>
      ) : searchMode === "general" ? (
        